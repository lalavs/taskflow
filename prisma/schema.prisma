generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Note {
  id      String @id @default(uuid())
  x       Float
  y       Float
  content String

  @@map("notes")
}
